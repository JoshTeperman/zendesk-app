import { getTickets, getPage, formatTicketData } from '../containers/App';

const mockData = {}
const mockResponse = {}
const mockTicketsArray = []

test.todo('fetch() is called once with the correct end-point'), () => {
// 'http://localhost:5000/tickets'
}

test.todo('fetch request to the API returns an object in the expected format with the correct number of tickets'), () => {
  // tickets, next_page, count
}

test.todo('formatTicketData() is called once with the parsed response object'), () => {
}

test.todo('formatTicketData() returns an Array in the expected format'), () => {
}
test.todo('getTickets() returns an array of ticket objects'), () => {
}

// --> Error handling 
